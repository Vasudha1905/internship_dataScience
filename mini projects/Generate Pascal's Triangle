## Generate Pascal's Triangle: Write a function to generate the first n rows of Pascal's triangle.

def pascal():
    n = int(input("Enter the number of rows: "))

    for i in range(n):
        # Print leading spaces
        print(" " * (n - i), end="")
        # Compute and print Pascal's Triangle row
        num = 1
        for j in range(i + 1):
            print(num, end=" ")
            num = num * (i - j) // (j + 1)
        
        print()

pascal()

